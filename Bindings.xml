<Bindings>
    <Binding name="Show" header="WowBeeTitle" category="WowBee">
		if WowBeeHelperFrame:IsShown() then
			WowBeeHelperFrame:Hide()
		else
			WowBeeHelperFrame:Show()
		end
    </Binding>
    <Binding name="StartStop" category="WowBee">
		if WowBee.AutoHelper.Start then
			WowBeeHelper_SetAutoScript(nil);
		else
			WowBeeHelper_SetAutoScript(true);
		end
    </Binding>
    <Binding name="Stop" category="WowBee">
		if(WowBee.AutoHelper.Start)then
			WowBeeHelper_SetAutoScript(nil);
		end
    </Binding>
</Bindings>